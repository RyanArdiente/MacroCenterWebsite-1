<title>{{page.title}}</title>
<h1>{{page.header}}</h1><hr>
<div>
  <div id="cart">
  </div>
  {{#each page.cart}}
    {{name}}
  {{/each}}
  <a href="/cart/checkout"><button class="btn btn-secondary btn-sm">Checkout</button></a>
  {{{page.script}}}
</div>
