<h1>{{page.header}}</h1>
<div>
  <table>
		{{#each page.links}}
      <tr>
        {{name}}
      </tr>
      <tr>
        {{email}}
      </tr>
      <tr>
        {{password}}
      </tr>
    {{/each}}
  </table>
</div>
{{script}}
