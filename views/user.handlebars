<h1>{{page.header}}</h1>
<div>
  <table>
		{{#each page.user}}
    <tr>
      <td>
        {{name}}
        {{email}}
        {{password}}
      </td>
    </tr>
    {{/each}}
  </table>
</div>
{{{script}}}
